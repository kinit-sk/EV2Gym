[project]
name = "ev2gym"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "gurobipy>=12.0.1",
    "gymnasium~=1.1",
    "networkx~=3.0",
    "numpy~=2.3",
    "pandas~=2.2",
    "pandapower",
    "multicopula",
    "numba~=0.61",
    "pyyaml>=6.0.2",
]
